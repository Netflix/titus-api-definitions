{
  "definitions": {
    "LeaderResponse": {
      "title": "Leader response",
      "type": "object",
      "properties": {
        "hostname": {
          "description": "host name",
          "type": "string"
        },
        "hostIP": {
          "description": "host ip",
          "type": "string"
        },
        "apiPort": {
          "description": "api port for the leader",
          "type": "integer"
        },
        "mesosLeader": {
          "description": "Mesos leader address in format '<host>:<port>",
          "type": "string"
        },
        "mesosServers": {
          "description": "List of Mesos server addresses, where each item is in format 'host:port'",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        }
      }
    }
  }
}